/**
 * LengthOfN.ts
 * @author HU JIAJUN <e0556371@u.nus.edu>
 * @file LengthOfN algorithm
 */

export const partialCode = `// n的长度
int l = `;

export const fullCode = `// n的长度
int l = (int) (Math.log10(n) + 1);`;
