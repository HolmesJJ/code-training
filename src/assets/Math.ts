/**
 * Math.ts
 * @author HU JIAJUN <e0556371@u.nus.edu>
 * @file Math algorithm
 */

export const partialCode = `// x的绝对值


// x^y, 返回double


// 返回x和y中的较大值


// 返回x和y中的较小值`;

export const fullCode = `// x的绝对值
Math.abs(x)

// x^y, 返回double
Math.pow(x, y)

// 返回x和y中的较大值
Math.max(x, y)

// 返回x和y中的较小值
Math.min(x, y)`;
